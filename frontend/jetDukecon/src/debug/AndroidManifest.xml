<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name="org.dukecon.android.ui.features.timemachine.SetCustomdateTimeReceiver"
            android:enabled="true">
            <intent-filter android:priority="999">
                <action android:name="org.dukecon.android.ui.intent.TIME" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
